# IAM UI Authentication Boundary

The organisation context uses canonical IAM organisation endpoints only. The legacy organisation fallback has been removed. The shared legacy Axios token client remains a separate migration item.
