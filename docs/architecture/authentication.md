# Authentication

Phase **A.1** deliverable. Foundation provides:

- `hashPassword` / `verifyPassword` (Argon2id)  
- Password policy  
- Token hashing  
- Identity create with password credential  

Planned routes:

`/auth/login` · `/auth/forgot-password` · `/auth/reset-password` · `/auth/verify-email`
