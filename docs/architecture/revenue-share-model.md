# Revenue Share Model

Configurable categories:

master · publishing · mechanical · performance · neighboring · sync · distribution · administration · streaming · custom

Mapped from Rights categories on promotion (`mapRightCategoryToRevenue`).

Shares attach to `RoyaltyAllocation` rows as `RevenueShare` beneficiaries with percent/weight/fixed.
